apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mynginx
  namespace: default
spec:
  interval: 10m
  chart:
    spec:
      chart: charts/mynginx
      sourceRef:
        kind: GitRepository
        name: github
        namespace: flux-system
      interval: 10m

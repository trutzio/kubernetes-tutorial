apiVersion: networking.k8s.io/v1
kind: Ingress	
metadata:	
  name: pgadmin4
spec:
  ingressClassName: traefik	
  rules:
    - host: cluster.trutz.cloud
      http:
        paths:
          - path: /pgadmin4
            pathType: Prefix
            backend:
              service:
                name: pgadmin4
                port:
                  number: 9090
apiVersion: v1
kind: Service
metadata:
  name: pgadmin4
spec:
  type: ClusterIP
  selector:
    app: pgadmin4
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 80
  #sessionAffinity: ClientIP
  #sessionAffinityConfig:
    #clientIP:
      #timeoutSeconds: 10800
  